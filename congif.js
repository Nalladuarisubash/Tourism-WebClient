

export default class Configuration {

    // URL for the backend API
    BACKEND_URL = "http://localhost:3000/api/v1"

    // Default username for backend authentication
    BACKEND_USERNAME = "admin"

    // Default password for backend authentication
    BACKEND_PASSWORD = "admin123"

    // Default creator of records in the system
    CREATED_BY = "System-User"

    // Default modifier of records in the system
    MODIFIED_BY = "System-User"
}
